onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }

onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
onst sendToBuildQuote = async () => {    const draft = new URLSearchParams(window.location.search).get('draft');      alert('Open this system from BuildQuote first');      return;    }    const items = [...panels, ...accessories].map((it) => ({      component_id: it.code,      manufacturer: title,      system: title,      sku: it.code,      name: it.name,      description: '',      uom: it.uom,      qty: it.qty || 1    }));    await fetch('https://expert-spork-697xj5vx9ggp3jj9-3000.app.github.dev/api/add-components', {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ draft_id: draft, items })    });    alert('Items added to BuildQuote. Return to RFQ tab.');  }
