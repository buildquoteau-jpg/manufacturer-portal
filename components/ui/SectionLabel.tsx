export default function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-orange-500 text-xs font-semibold uppercase tracking-widest mb-2">{children}</p>
  )
}