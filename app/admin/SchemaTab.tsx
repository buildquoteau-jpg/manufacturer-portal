'use client'

export default function SchemaTab() {
  return (
    <iframe
      src="/schema-mindmap.html"
      className="w-full border-0"
      style={{ height: 'calc(100vh - 113px)' }}
      title="Database Schema Mind Map"
    />
  )
}
